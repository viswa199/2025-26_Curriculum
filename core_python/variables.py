# Variables x, y, and name store values of different types, such as integers, text, or floats.
# String must be wrapped in either double or single quotes.
# The variable must not have a space in between.
# The variable must not start with a number.
# The variable must not have special characters except _.

# Task1: Uncomment the line 16 & 17 and run the code. Resolve the errors.

x = 3 
y = 5
print(x + y)

name = 'Johny'
print(name)

# 1 = 'gieg'
# age$ = '12'
#print(1)
#print(age$)
