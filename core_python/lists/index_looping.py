

thislist = ["apple", "banana", "cherry"]

#calculate the length of the list.
length_of_thislist = len(thislist)

#we are using the range function to loop through the list.
#we are looping through the list using the index of the list.
for i in range(length_of_thislist):
  print(thislist[i])


  